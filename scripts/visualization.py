"""Computes and visualizes the results of the experiments."""
from skfp.fingerprints import ECFPFingerprint
from tsnecuda import TSNE

@Cache(

)
def decompose(
    fingerprint: np.ndarray
) -> np.ndarray:
    """Return TSNE decompositions"""

@Cache(
    
)
def fingerprints(
    fingerprint: 
) -> np.ndarray:
    """Return TSNE decompositions"""
